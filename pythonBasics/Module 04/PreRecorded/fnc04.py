"""
Lambda Functions
    - small, anonymous, inline
    - lambda keyword
    - simple lambda function
    - lambda function params
    - lambda function simple condition

Use case
    - simple functions
    - functions that are not reused
    - inline use
    - readability

When Not
    - complex
    - reusability
"""
""" 
result = lambda: 3+12
print(result())
"""

result = lambda x, y: x+y
print(result(10, 40))
